# Python
__pycache__/
*.py[cod]
*$py.class
venv/
.venv/

# Environment variable files — keep .env.example files (they are documentation)
.env
.env.local
.env.production

# SQLite database — gitignored because Render auto-seeds on startup
*.db
*.sqlite3

# NOTE: *.pkl ML model files are intentionally NOT gitignored.
# They must be committed so Render can load them at runtime.

# Node / React
node_modules/
dist/
dist-ssr/
*.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# IDEs
.vscode/
.idea/
.DS_Store
